<div class="mx-auto max-w-7xl">
  <!-- ゲーム開始セクション -->
  <div class="text-center mb-16 mt-16">
    <h1 class="text-4xl font-bold mb-8">
      <span class="text-gray-800">準備はいいですか？</span>
    </h1>

    <div class="mb-12">
      <p class="text-lg text-gray-600 mb-6 leading-relaxed max-w-2xl mx-auto">
        60秒間でできるだけ多くのRubyメソッドをタイピングしてください。<br>
        タイピング速度とRubyの知識を同時に鍛えましょう！
      </p>
    </div>

        <!-- ゲーム開始カード -->
    <div class="max-w-md mx-auto" data-controller="game-starter">
      <%= form_with url: games_path, method: :post, data: { game_starter_target: "form" } do |f| %>
        <div class="bg-white rounded-xl shadow-lg p-8 border border-gray-100 hover:shadow-xl transition-shadow duration-300">
          <h2 class="text-2xl font-bold text-gray-800 mb-4">ゲームスタート</h2>

          <p class="text-gray-600 mb-6 leading-relaxed">
            <kbd class="px-2 py-1 text-sm font-semibold text-gray-800 bg-gray-100 border border-gray-200 rounded-lg">Space</kbd>
            または
            <kbd class="px-2 py-1 text-sm font-semibold text-gray-800 bg-gray-100 border border-gray-200 rounded-lg">Enter</kbd>
            キーを押してスタート
          </p>

          <div class="text-sm text-gray-500 bg-gray-50 rounded-lg p-4">
            <div class="flex items-center justify-center mb-2">
              <%= heroicon "information-circle", variant: :outline, options: { class: "w-3.5 h-3.5 text-gray-400 mr-2" } %>
              ヒント
            </div>
            <p class="text-xs">ゲーム中に <kbd class="px-1 py-0.5 text-xs font-semibold text-gray-600 bg-white border border-gray-300 rounded">Esc</kbd> キーを押すと、いつでもやり直しできます</p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
